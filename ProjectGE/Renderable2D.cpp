#include "Renderable2D.h"


/*Sprite::Sprite(Transform transform, Texture2D texture, const GLuint shaderID, const glm::vec4 color) : transform(transform), texture(texture), shaderID(shaderID), color(color) {
	size = glm::vec2(texture.width, texture.height);
}
Sprite::Sprite(Transform transform, float width, float height, Texture2D texture, const GLuint shaderID, const glm::vec4 color) : transform(transform), size(width, height), texture(texture), shaderID(shaderID), color(color)
{
}


Sprite::~Sprite()
{
}

const GLuint& Sprite::getShaderID() const {
	return shaderID;
}
const GLuint& Sprite::getTextureID() const {
	return texture.ID;
}

const glm::vec2& Sprite::getPosition() const {
	return transform.getPosition();
}

const glm::vec4& Sprite::getColor() const {
	return color;
}

const glm::vec2& Sprite::getSize() const {
	return size;
}

const Transform& Sprite::getTransform() const {
	return transform;
}*/